# Install

```sh
go install github.com/rangertaha/web-mcp/cmd/web@latest
```

This produces a `web` binary. Or build from source:

```sh
git clone https://github.com/rangertaha/web-mcp
cd web-mcp
make build        # produces ./bin/web
```
